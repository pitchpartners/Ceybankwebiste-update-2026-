// Re-export the generated Prisma client so consumers can access runtime enums
// (e.g. FundReportType) and model types from a single entrypoint.
export * from "@prisma/client";
// export { Prisma, PrismaClient } from "../generated/prisma";
